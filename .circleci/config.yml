version: 2
jobs:
  build:
    machine: 
      image: ubuntu-2004:202010-01
    steps:
      - run:
          name: dependencies
          command: sudo apt update 
      - run:
          name: dependencies
          command: sudo apt install transmission-cli ffmpeg git python3 
